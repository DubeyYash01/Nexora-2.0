{
  "name": "nexora-root",
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd artifacts/nexora && pnpm install --no-frozen-lockfile && pnpm run build && cd ../api-server && pnpm install --no-frozen-lockfile && pnpm run build",
    "start": "cd artifacts/api-server && node --enable-source-maps ./dist/index.mjs"
  }
}
